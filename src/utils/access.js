import { utils } from "@telesero/frontend-common";

export const hasPermission = utils.access.hasPermission;
export const hasRole = utils.access.hasRole;
export const canShowResource = utils.access.canShowResource;
export const canAccessMenuItem = utils.access.canAccessMenuItem;
export const canAccessRoute = utils.access.canAccessRoute;
export const menuHasAccessibleItems = utils.access.menuHasAccessibleItems;
